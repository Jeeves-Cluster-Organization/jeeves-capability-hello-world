# Kustomize base for single-node deployment
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml
  - configmap.yaml

commonLabels:
  app.kubernetes.io/name: jeeves-hello-world
  app.kubernetes.io/component: orchestrator

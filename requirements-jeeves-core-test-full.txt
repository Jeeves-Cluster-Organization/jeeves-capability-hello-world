# Full Test Requirements for Jeeves-Core
# Install these to run ALL tests including optional LLM providers

# Base test requirements (always needed)
pytest>=7.0
pytest-asyncio>=0.21
pytest-cov>=4.0
pytest-timeout>=2.4.0
pytest-mock>=3.15.0

# Optional LLM Provider SDKs
# These are optional in production but needed for full test coverage
openai>=1.0.0
anthropic>=0.18.0
azure-ai-inference>=1.0.0

# Distributed system support
redis>=4.5.0

# Database testing
testcontainers>=3.7.0
asyncpg>=0.29.0

# HTTP/API testing
httpx>=0.26.0
respx>=0.22.0

# ML dependencies (optional, for memory module semantic search tests)
# sentence-transformers>=2.2.0  # 1.5GB+ - uncomment if needed

# Note: Most tests will pass without these optional dependencies
# They are intentionally optional to allow lightweight production deployments

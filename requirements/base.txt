# ============================================================================
# Base Requirements - Core runtime dependencies
# ============================================================================

# Core utilities
pydantic>=2.5.0,<3.0.0
pydantic-settings>=2.0.0
structlog==23.2.0
httpx==0.27.2

# gRPC (required by jeeves_infra.kernel_client — module-level import)
grpcio>=1.60.0

# UI
gradio>=4.0.0,<5.0.0
huggingface_hub>=0.20.0,<0.25.0

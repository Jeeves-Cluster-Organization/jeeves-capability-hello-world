# ============================================================================
# Optional Requirements - Cloud providers and monitoring
# ============================================================================
# These are NOT required for baremetal llama-server deployment.
# Install only if you need specific providers or monitoring.
#
# The codebase has graceful fallbacks for all of these:
#   - llm/provider.py: try/except for openai, anthropic
#   - common/metrics.py: noop fallback for prometheus-client
#
# Usage:
#   pip install -r requirements-optional.txt   # Install all optional
#   pip install openai                         # Install specific provider
# ============================================================================

# Cloud LLM Providers (optional - graceful fallback in llm/provider.py)
openai>=1.0.0          # OpenAI API (GPT-4, GPT-3.5) + Azure OpenAI
anthropic>=0.18.0      # Anthropic API (Claude)

# Monitoring (optional - graceful fallback in common/metrics.py)
prometheus-client==0.19.0

# Kubernetes (optional - K8s LLM registry)
kubernetes==29.0.0
PyYAML==6.0.1
